# test token, doesn't really matter
LLM_BEARER_TOKEN=llm-local-dev-token-change-me
LLM_MODEL=llama3.2

# on  = HTTPS, needs ./certs/generate-certs.sh, pair with LLM_PORT=8443
# off = plain HTTP, no certs needed, pair with LLM_PORT=8442
LLM_TLS=on
LLM_PORT=8443
